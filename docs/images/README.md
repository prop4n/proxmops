# Screenshots

Drop PNG screenshots here with these exact names so the main README picks them up:

- `overview-graph.png` - the overview in graph (schema) mode
- `overview-cards.png` - the overview in card mode
- `resource-detail.png` - the right-hand detail drawer (desired vs observed, history)
- `logs.png` - the daemon logs page

Any size works; the README displays them full width.
